import os
from dotenv import load_dotenv

# Загружаем секреты из .env
load_dotenv()

# === БАЗОВЫЕ НАСТРОЙКИ ===
# ВАШ РЕАЛЬНЫЙ БЛОГ:
BLOGGER_RSS_BASE = 'https://blog.roses-crimea.ru/feeds/posts/default'

# === ВАШИ МЕТКИ ===
BLOGS = [
    # Основные метки для вашего блога о розах
    {
        'name': 'rose_recipes',
        'label': 'recipes',  # Рецепты с розами
        'enabled': True,
        'tags': ['рецепты', 'розы', 'кулинария', 'крымские_розы']
    },
    {
        'name': 'centifolia',
        'label': 'Centifolia',  # Столистная роза
        'enabled': True,
        'tags': ['centifolia', 'столистная_роза', 'крым']
    },
    {
        'name': 'crimea_roses', 
        'label': 'Crimea',  # Крымские розы
        'enabled': True,
        'tags': ['крым', 'розы', 'crimea', 'крымские_розы']
    },
    {
        'name': 'youtube_videos',
        'label': 'youtube',  # YouTube видео с канала
        'enabled': True,
        'tags': ['youtube', 'видео', 'рецепты', 'видеорецепты']
    },
    {
        'name': 'rose_oil',
        'label': 'масло шиповника',  # Масло шиповника
        'enabled': True,
        'tags': ['косметика', 'масло', 'розы', 'уход']
    },
    {
        'name': 'all_posts',
        'label': None,  # ВСЕ посты (без фильтра)
        'enabled': False,  # Сначала отключим, потом включим
        'tags': ['все_темы', 'архив']
    }
]

# === TELEGRAM ===
TELEGRAM_BOT_TOKEN = os.getenv('TELEGRAM_BOT_TOKEN', '')
TELEGRAM_CHANNEL = os.getenv('TELEGRAM_CHANNEL', '@ваш_канал')

# === НАСТРОЙКИ ===
SETTINGS = {
    'max_posts_per_fetch': 20,
    'archive_enabled': True,
    'telegram_preview_length': 800,
    'check_interval_hours': 6  # Проверять каждые 6 часов
}